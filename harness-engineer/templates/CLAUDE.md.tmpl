# {{PROJECT_NAME}}

> {{PROJECT_TAGLINE}}

## 技術棧
{{TECH_STACK}}

## 語言規則
- 所有對話、commit、文件、程式碼註解使用**繁體中文**

## 行為約束
- 不得執行破壞性 git 指令（`reset --hard`、`push --force`）除非使用者明示
- 不得 commit secrets（.env、credentials.json、access token）
- 修改超過 5 個檔案前，先更新 `plan.md`
- session 結束前更新 `status.md`，含「下次第一步」

## 子文件地圖

| 需求 | 路徑 |
|---|---|
| 詳細架構 | `docs/architecture.md` |
| 安裝步驟 | `docs/install.md` |
| 使用方式 | `docs/usage.md` |
| 過去錯誤 | `learnings/INDEX.md` |
| 任務計畫 | `plan.md` |
| 當前進度 | `status.md` |
| **工項追蹤** | **`input/backlog.md`** |
| 設計決策 | `docs/adr/` |
| 成果素材 | `notes/` |

## Session 啟動 SOP
1. 讀 `status.md` → 找到當前工項 ID
2. 查 `input/backlog.md` → 確認該工項依賴是否完成、狀態是否可開工
3. 讀 `learnings/INDEX.md`，命中相關情境再展開該張卡
4. 讀 `plan.md` 確認當前 Phase 範圍
5. 開工

## 經驗教訓卡
- 教訓寫成獨立卡片放 `learnings/cards/`，**不要合併成單一檔案**（多人推 git 會衝突）
- 新增用 `scripts/new-learning-card.ps1 -Title "標題"`，檔名自動帶時間與作者
- `learnings/INDEX.md` 由腳本產生，勿手改；衝突時重跑腳本覆蓋

## 重要規則
- **工項狀態只改 `input/backlog.md`**，不改 phase 細節檔（phase 檔只存規格說明）
- 取消工項用刪除線 + 🚫 保留，不刪除行
- 新增工項先在 `input/backlog.md` 給定唯一 ID，再在細節檔補驗收條件
