# 經驗教訓卡 — {{PROJECT_NAME}}

> **For Agent，非 for human。** 每次 session 開始時 Agent 應先讀 `INDEX.md`，
> 命中相關情境再展開對應卡片，避免重複犯錯浪費 token。

---

## 為什麼是「一張卡一個檔」

這個目錄採多檔案收納，而不是把所有教訓寫在同一份 `learnings.md`，原因是**團隊協作要推 git**：

| 單一檔案 | 一張卡一個檔 |
|---|---|
| 兩人同時新增教訓 → 必定衝突 | 各自新增不同檔案 → 不會衝突 |
| 衝突要人工合併，容易貼錯位置 | 幾乎不需要合併 |
| 檔案越長越沒人讀 | 可依標籤、嚴重度挑著讀 |

---

## 目錄結構

```
learnings/
├── README.md      # 本檔：規則說明
├── INDEX.md       # 卡片索引（由腳本產生，不要手改）
└── cards/         # 一張卡一個檔
    └── YYYYMMDD-HHmm-作者-短標題.md
```

---

## 檔名規則

```
YYYYMMDD-HHmm-<作者>-<短標題>.md
```

| 段落 | 說明 | 範例 |
|---|---|---|
| `YYYYMMDD-HHmm` | 建立時間，精確到分鐘 | `20260904-1132` |
| `<作者>` | 推送者代號，取自 `git config user.name` | `golden` |
| `<短標題>` | 一眼看懂這張卡在講什麼 | `工項狀態分散多檔` |

**時間與作者是必要的**：同一天多人建卡時，靠這兩段就能避開檔名碰撞，
在 git 記錄裡也能直接看出是誰、什麼時候留下的經驗。

---

## 怎麼新增一張卡

**方式一：用腳本**（會自動帶入時間與 git 使用者名稱）

```powershell
.\.claude\skills\harness-engineer\scripts\new-learning-card.ps1 -Title "工項狀態分散多檔"
```

**方式二：手動**

複製 `cards/` 底下任一張卡，改檔名與內容即可。

新增後執行下列指令更新索引：

```powershell
.\.claude\skills\harness-engineer\scripts\build-learning-index.ps1
```

---

## 卡片格式

每張卡開頭是一段 YAML front matter，索引腳本靠它產生 `INDEX.md`：

```yaml
---
id: 20260904-1132-golden-工項狀態分散多檔
date: 2026-09-04 11:32
author: golden
project: {{PROJECT_NAME}}
tags: [工程管理, 狀態管理]
severity: high        # high | medium | low
status: active        # active | archived
---
```

正文固定五段，缺一不可：

| 段落 | 寫什麼 | 為什麼要有 |
|---|---|---|
| 情境 | 什麼時候、在做什麼 | 讓 Agent 判斷是否命中當前情境 |
| 錯誤 | 實際發生什麼問題 | 具體現象，不要寫「有問題」 |
| 根因 | 為什麼會這樣 | 沒有根因就只是抱怨 |
| **規避規則** | 下次怎麼避開（**必填、可執行**） | 這是整張卡的價值所在 |
| 相關 | 關聯的其他卡片 id | 建立脈絡 |

---

## 索引怎麼維護

`INDEX.md` **由 `build-learning-index.ps1` 產生，不要手動編輯**。

多人同時更新索引時 git 仍可能衝突。發生衝突時**不要手動合併**，
直接重新執行一次腳本覆蓋即可 —— 索引的內容完全由 `cards/` 推導而來。

---

## 卡片的生命週期

| 狀態 | 意義 | 處理 |
|---|---|---|
| `active` | 現行有效 | 出現在索引主表 |
| `archived` | 已不適用（技術棧更換、流程調整） | 索引另列一區，卡片不刪除 |

**不要刪除卡片。** 過時的改成 `archived` 並在正文補一行說明為什麼不再適用，
保留下來才看得出決策的演變。

---

## 什麼情況該建卡

- 同一個錯誤犯了第二次
- 花了超過半小時才查出原因
- 踩到的坑跟環境、工具行為有關（不是單純的邏輯 bug）
- 做了取捨，日後可能有人想改回去

反過來說，**單純的程式 bug 修好就好，不必建卡**。
卡片是給未來的自己與同事看的，不是錯誤紀錄簿。
