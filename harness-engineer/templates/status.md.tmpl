# {{PROJECT_NAME}} 狀態

## 最後更新：{{TODAY}}

## 進行中任務

- [ ] [plan.md Phase 1] 任務描述

## 已完成 checkpoint

（依時間倒序）
- _尚無_

## 下次啟動第一步

（重啟 session 後 Agent 應該從這裡接手）

例：打開 `src/xxx.ts`，實作 `fooBar()` 函數的 edge case 處理。

## Blocker

- 無

## Git 狀態

- branch: main
- HEAD: （由 stop hook 自動填入）

## 備忘

（任何需要下個 session 知道的事）

---

> 此檔案會由 `stop-status-snapshot.ps1` hook 自動更新「最後更新」「Git 狀態」欄位。
> 「下次啟動第一步」與「Blocker」需手動維護。
