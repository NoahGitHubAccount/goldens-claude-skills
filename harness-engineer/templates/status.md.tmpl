# {{PROJECT_NAME}} 狀態

## 最後更新：{{TODAY}}

## 當前工項

🔨 **?** — （填入工項 ID 與說明，詳見 `input/backlog.md`）

## 下一步指令

1. 查 `input/backlog.md` 確認此工項依賴已完成
2. （具體行動，例：打開 `src/xxx.ts` 實作 `fooBar()`）
3. 驗收：符合 `input/backlog.md` 對應 ID 的驗收條件

## Blocker

- 無

## Git 狀態

- branch: main
- HEAD: （由 stop hook 自動填入）

## 備忘

（任何需要下個 session 知道的事）

---

> 此檔案只存「當前工項 ID 指針」與「下一步指令」。
> 工項完成狀態改在 `input/backlog.md`，已完成 checkpoint 移至 `status-history/`。
> `stop-status-snapshot.ps1` hook 自動更新「最後更新」「Git 狀態」欄位。
