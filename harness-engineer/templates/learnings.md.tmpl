# Agent Learnings — {{PROJECT_NAME}}

> **For Agent，非 for human。** 每次 session 開始時 Agent 應主動讀取此檔，避免重複犯錯浪費 token。

---

## Top 教訓索引（最重要的放這）

（保留最多 10 條，新教訓加在這裡，舊的下沉到下方歸檔區）

- [2026-05-17] 工項狀態分散多檔 → 編號錯位、需改兩處 → 集中在 `input/backlog.md`

---

## 詳細條目

### 條目格式

```
[YYYY-MM-DD] 一行情境摘要
- 情境：什麼時候、做什麼
- 錯誤：實際發生什麼問題
- 根因：為什麼會錯
- 規避規則：下次怎麼避（必填，可執行）
```

### 條目

[2026-05-17] 工項狀態分散在多個 phase 檔 + status.md
- 情境：多 Phase 專案，phase-A/B/C 各有 checkbox，status.md 同時描述進度
- 錯誤：編號系統不一致（A1~A6 vs A.1~A.4），session 啟動需人工對照，仍對應錯誤
- 根因：沒有單一 source of truth，完成一項需更新兩個地方
- 規避規則：工項狀態集中在 `input/backlog.md`；phase 細節檔只存規格說明，不含 checkbox；status.md 只存當前工項 ID 指針

---

## 待整理（Hook 自動寫入區）

`post-bash-learning.ps1` hook 會把失敗指令摘要寫到這裡，使用者整理後移到「詳細條目」區或刪除。

_尚無_

---

## 歸檔

超過 6 個月未觸發的條目移到 `learnings.archive.md`。
